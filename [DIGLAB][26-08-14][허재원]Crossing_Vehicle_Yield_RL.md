# 진입 차량 대응 RL — 접근 속도 제한

## 요약 (10줄 이내)

**지난 지시 (2026-08-13)** — 키워드 3줄
- 진입 차량(가로지르는 차·끼어드는 차)은 별도 과제로 분리
- 결과가 안 나오면 자료를 찾아 재설계하고 학습·평가까지 진행
- 고속 교차는 현실에 드물지만 감지했으면 대처는 해야 함

**합의 사항 → 상태**
- [완료] 접근 속도 제한 도입 — 불필요한 지연 +12.6 m → **+1.6 m**
- [완료] 고속 구간 교차 배제 — 경로 기준속도 61 km/h 이하에만 배치
- [부분] 겹침 충돌 — 저속에서 0/20 ↔ 6/20 으로 두 설계가 상반
- [진행] 제한이 스스로 풀리는 문제를 고쳐 재학습 중

**이번 결과 / 막힌 것 / 다음**
- 결과: 지연 +1.6 m, 복귀 ctp 0.090 m (이전 0.161)
- 막힌 것: 충돌과 지연이 맞바꿈 관계로 나옴 **판단 필요** 🔴
- 다음: 감속해도 접근 제한이 풀리지 않게 고친 `hz_o` 학습·평가

---

## 1. 설계

한 줄 요약: **정책이 2~3초 제동을 유지하게 만드는 대신, 환경이 "설 수 있는 속도"로 눌러 접근시키고 정책은 갈지 말지만 정한다.**

정지 장애물과 진입 차량은 정답이 다름.

| | 정지 장애물 | 진입 차량 |
|---|---|---|
| 정답 | 옆으로 1.2 m 비켜감 | 도착 시각을 바꿈 (감속 또는 가속) |
| 필요한 시간 | 조향 한 번, 0.2~1초 | 제동 **2~3초 유지** |
| 옳고 그름 | 항상 피하는 게 정답 | 타이밍에 따라 다름 |

- 정지물의 CPA(최근접 거리)는 순수 기하량이라 감속해도 안 변하고 조향해야만 커짐 -> 보상의 기울기가 조향만 가리키고 한 스텝으로 효과가 남
- 진입 차량은 감속으로도 CPA 가 커지지만 **80스텝을 연속으로 밟아야** 효과가 남 -> 무작위 탐색으로는 나오지 않음

그래서 감속 시작 시점을 기하로 고정.

```
v_allow = √(2·a·d)      a = 3.0 m/s² (설계 제동 6.0 의 절반),  d = 교차점까지 거리
```

- 충돌 코스일 때만 적용(예측 CPA < 3.4 m). 관측 범위의 모든 차에 걸면 감지할 때마다 기어감 (실측 평균속도 20 → 6 m/s)
- 하한 2 m/s — 완전히 서지 않고 다가가서 보게
- 정책에 남는 결정은 **그대로 설지 / 가속해 먼저 갈지** 하나

**고속 구간에는 교차를 배치하지 않음** — 90 km/h 에서 제동거리 52 m, 갈등 창 0.44초라 물리적으로 못 푸는 문제. 계속 학습시키면 저속 구간 학습까지 오염됨. 경로 기준속도 17 m/s(61 km/h) 이하에만 배치.

---

## 2. 결과

`hz_k` = 접근 제한 없음(정책이 감속을 전담), `hz_n` = 접근 제한 도입. 40셀 기준.

| | hz_k it2000 | hz_n it4000 |
|---|---|---|
| 안 겹침 지연 | +12.6 m | **+1.6 m** |
| 복귀 ctp | 0.161 m | **0.090 m** |
| 겹침 충돌 (전체) | 9/40 | 17/40 |

속도대로 나누면 성격이 갈림. 겹침 조건, 사전 인지(가시 80 m).

| 경로군 | hz_k 충돌 | hz_n 충돌 | hz_k 지연 | hz_n 지연 |
|---|---|---|---|---|
| 저속 (`shp_*`) | **0/20** | 6/20 | +7.4 m | **+4.6 m** |
| 고속 (`hsnew*`) | 9/20 | 11/20 | +19.7 m | **+14.8 m** |

- 두 설계가 정반대 지점에 있음. hz_k 는 많이 줄여 저속에서 안 부딪히지만 불필요하게 느리고, hz_n 은 거의 안 줄여 빠르지만 부딪힘
- 눈가림 대조군(같은 정책에서 장애물 관측만 가림)과 비교하면 고속에서 방향이 다름  
-> hz_k 는 9/20 대 대조군 2/20 으로 **정책이 더 부딪힘**. hz_n 은 11/20 대 14/20, 가림 조건은 6/20 대 20/20

---

## 3. 성공한 부분과 이유

**불필요한 지연이 사라짐** (+12.6 → +1.6 m).

- 이전 설계는 정책이 감속을 전담해서, 한 번 밟기 시작하면 언제 놓을지도 학습해야 했음. 안 겹치는 상황에서도 서 버림
- 접근 제한은 거리 함수라 **교차점을 지나면 자동으로 풀림**. 놓는 시점을 배울 필요가 없음

**추종이 좋아짐** (복귀 ctp 0.161 → 0.090 m).

- 감속을 환경이 맡으면서 잔차가 조향에만 쓰임 -> 통과 후 경로 복귀가 빨라짐

---

## 4. 실패 원인 가설

한 줄 요약: **제한을 걸지 말지의 판정을 현재 속도로 해서, 줄이기 시작하면 스스로 풀림.**

접근 제한은 항상 켜져 있는 것이 아니라 "지금 이대로 가면 부딪히나"를 매 스텝 판정해서 켬. 그 판정에 쓰는 속도가 문제.

| 판정에 쓰는 속도 | 감속하면 |
|---|---|
| 현재 속도 (hz_n) | 부딪힐 예측이 사라짐 -> 조건이 꺼지고 **제한 해제** |
| 경로 기준속도 (`hz_o`) | 감속과 무관하게 유지 -> 제한 유지 |

- 제한이 풀리면 다시 가속해 또 부딪힐 상황이 됨. 효과가 나는 순간 자기가 자기를 해제하는 구조
- hz_n 의 지연이 +0.5 m 로 거의 0 인데 저속 충돌이 0/20 → 6/20 으로 는 것이 이 증상과 맞음
- 같은 함정을 두 번 겪음. 회피 게이트에서도 "비켜가는 중에 부족분으로 계산하면 게이트가 스스로 닫힌다"는 기록이 있음

**대응**: 판정을 **자차가 기준속도로 계속 갔다면 부딪혔을지**로 바꿈. 감속해도 값이 안 변하므로 제한이 유지됨. 같은 기준을 보상에 썼을 때는 역효과였으나(판정이 오래 켜져 지연만 늘어남), 여기서는 환경 제한의 유지 조건이라 역할이 다름.

---

## 5. 측정에서 고친 것

🔴 이번에 "성공"으로 보고했다가 두 번 뒤집힘. 둘 다 측정 결함이었음.

| 결함 | 내용 | 영향 |
|---|---|---|
| 집계 키 누락 | 셀을 묶는 키에 진입 유형이 빠져 4종이 덮어써짐 | 40셀 중 **10셀만** 보고 있었음 |
| 학습·평가 env 불일치 | 학습은 양보 지령에 1차 지연이 걸린 env, 평가는 그 필터가 없는 env | **학습 때와 다른 차**를 평가 |

- 두 번째를 고치니 `hz_k` 의 겹침 충돌이 2/40(대조군 4) → **9/40(대조군 7)** 로 뒤집힘. 정본으로 보고했던 근거가 사라짐
- 평가기가 학습 설정 파일을 읽어 같은 env 를 만들도록 수정. 학습 인자를 설정 파일에 **기록하는 시점**도 함께 수정(기록이 비면 고친 평가기도 옛 env 를 만듦)

---

## 6. TODO

- [ ] **`hz_o`** — "원래 속도로 갔다면 부딪혔을지"로 판정을 바꿔 감속 중에도 접근 제한이 유지되게 한 재학습. 목표는 저속 겹침 충돌 0~2/20 과 지연 +2 m 이내를 동시에
- [ ] **선행 차량(느린 앞차)** — 합류 차량이 도로에 올라탄 뒤 8 m/s 로 가는 것을 20 m/s 자차가 뒤에서 받는 충돌이 6/40. 대조군도 6/6 동일해서 정책이 손댈 수 있는 영역이 아님. 차간거리 유지가 보상에 없음
- [ ] **고속 교차의 목표 정의** — 물리적으로 못 피하는 구간에서 무엇을 정답으로 둘지. 현재는 충돌 속도에 비례하는 페널티로 "못 피하면 최대한 줄이고 부딪힘"만 유도
