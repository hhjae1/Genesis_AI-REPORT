# 진입 차량 대응 RL — 접근 속도 제한

## 요약 (10줄 이내)

**지난 지시 (2026-08-13)**
- 진입 차량(가로지르는 차·끼어드는 차)은 별도 과제로 분리
- 고속 교차는 현실에 드물지만 감지했으면 대처는 해야 함

**합의 사항 → 상태**
- [완료] 접근 속도 제한 도입 — 감속 시점을 기하로 고정
- [완료] 고속 구간 교차 배제 — 경로 기준속도 61 km/h 이하에만 배치
- [부분] 겹침 충돌 — 눈가림 20/40 대비 14/40, 가림 조건은 15/40 대비 7/40
- [진행] 제한이 스스로 풀리는 문제를 고쳐 재학습 중

**이번 결과 / 막힌 것 / 다음**
- 결과: 불필요한 뒤처짐 +0.0~2.7 m, ctp 0.08~0.18 m 로 추종 유지. 겹침 충돌은 14/40 로 아직 큼
- 막힌 것: 접근 제한이 스스로 꺼지고, 저속에서는 잘 안 걸림 **판단 필요** 🔴
- 다음: 감속해도 접근 제한이 풀리지 않게 고친 `hz_o` 학습·평가

---

## 1. 설계

한 줄 요약: **정책이 2~3초 제동을 유지하게 만드는 대신, 환경이 "설 수 있는 속도"로 눌러 접근시키고 정책은 갈지 말지만 정한다.**   
-> 즉, 오는 차량을 미리 감지하고 감속을 하여 교차점에서 정지할지 아니면 그냥 그대로 지나갈지를 판단.

![그림 1 — 진입 차량 유형](images_jw/hz_entry_types.png)

**그림 1.** 시뮬레이터 화면. 진입 차량은 자차 경로를 가로지르거나 경로로 끼어드는 차. 정지물처럼 서 있는 게 아니라 자차 도달 시각에 맞춰 교차 지점(검은 링)을 지나감.

둘 다 피해야 하는 건 같고, **피하는 방법**이 다름.

| | 정지 장애물 | 진입 차량 |
|---|---|---|
| 피하는 방법 | 옆으로 1.2 m 비켜감 | 속도를 줄여 보내거나, 아직 멀면 먼저 지나감 |
| 필요한 시간 | 조향 한 번, 0.2~1초 | 제동 **2~3초 유지** |
| 언제 필요한가 | 장애물이 있으면 항상 | 자차보다 늦게 오는 차면 안 줄여도 됨 |


### 접근 속도 제한

충돌 코스로 판정된 동안 PID 의 목표속도를 아래 값으로 눌러 두는 것.

```
v_allow = √(2·a·d)      a = 3.0 m/s²(설계 제동 6.0 의 절반),  d = 교차점까지 거리
```

-> "감속하라"가 아니라 **"이 속도를 넘지 마라"**. 남은 거리가 줄면 상한도 같이 내려가므로 결과적으로 천천히 감속하며 접근하게 됨

| 교차점까지 거리 | 속도 상한 |
|---|---|
| 50 m | 17.3 m/s (62 km/h) |
| 20 m | 11.0 m/s (39 km/h) |
| 5 m | 5.5 m/s (20 km/h) |
| 1 m | 2.4 m/s (8.6 km/h) |

-> 어느 지점에서든 이 값을 지키고 있으면 남은 거리 안에서 멈출 수 있음. 그래서 미리 밟아둘 필요 없이 **교차점 앞까지 가서 판단해도 늦지 않음**
-> 상한이므로 지금 속도가 이미 그보다 낮으면 아무 일도 안 일어남

- 충돌 코스일 때만 적용(예측 최근접 거리 < 3.4 m)  
-> 관측 범위에 들어온 모든 차에 걸면 감지할 때마다 기어감. 실측으로 갈등이 잡힌 구간의 평균속도가 **6 m/s** 까지 떨어짐(그 구간 기준속도는 20 m/s 대)
- 하한 2 m/s — 완전히 서지 않고 다가가서 보게
- 정책에 남는 결정은 **그대로 설지 / 가속해 먼저 갈지** 하나  
-> 교차점 근처까지 갔는데 진입 차량이 아직 멀면 세울 이유가 없음. 이때 가속해 지나가는 것이 후자

>**고속 구간에는 교차를 배치하지 않음**   
> - 90 km/h 에서 제동거리 52 m, 갈등 창 0.44초라 물리적으로 못 푸는 문제. 계속 학습시키면 저속 구간 학습까지 오염됨. 경로 기준속도 17 m/s(61 km/h) 이하에만 배치.   
> - 또한, 현실 상황에서도 고속 교차는 거의 존재하지 않거나 실제로도 대처가 거의 안되는 문제이기에 배제함.

---

## 2. 결과

최신 정책 `hz_n it4000`. 시험 경로 10개 × 진입 유형 4종 = 40셀, 지터·가시거리 조건마다 따로.

| 조건 | 충돌 | 눈가림 | ctp [m] | along [m] | v_err [m/s] |
|---|---|---|---|---|---|
| 가시 80 m · 겹침 | 14/40 | 20/40 | 0.179 | +6.1 | 0.169 |
| 가시 80 m · 안 겹침(−1s) | 6/40 | 6/40 | 0.089 | +2.6 | 0.068 |
| 가시 80 m · 안 겹침(+1s) | 3/40 | 2/40 | 0.104 | +2.7 | 0.078 |
| 가시 30 m · 겹침 | **7/40** | 15/40 | 0.150 | +9.3 | 0.133 |
| 가시 30 m · 안 겹침(−1s) | 4/40 | 6/40 | 0.096 | +0.7 | 0.068 |
| 가시 30 m · 안 겹침(+1s) | 2/40 | 0/40 | 0.082 | +0.0 | 0.112 |

- 겹치는 조건에서 눈가림 대비 20 → 14, 15 → 7 로 줄었으나 **절대값이 여전히 큼**
- 안 겹치는 조건의 뒤처짐이 +0.0~2.7 m — 불필요한 정지는 거의 없음
- ctp 0.08~0.18 m, v_err 0.07~0.17 m/s 로 추종은 유지됨

**영상** — [video_jw/hz_entry_hzn.mp4](video_jw/hz_entry_hzn.mp4). `shp_scurve` 경로, 직각 횡단 차량이 자차 도달 시각에 맞춰 교차점을 지나는 상황. 56초.

---

## 3. 설계가 맞았던 부분

**놓는 시점을 학습할 필요가 없어짐.**

- 정책이 감속을 전담하면 밟는 시점과 **놓는 시점**을 둘 다 배워야 함. 안 겹치는 상황에서도 계속 서 있는 실패가 여기서 나옴
- 접근 제한은 거리 함수라 교차점을 지나면 **자동으로 풀림**

**감속과 조향이 분리됨.**

- 감속을 환경이 맡으면서 잔차는 조향에만 쓰임 -> 통과 후 경로 복귀가 빨라짐

---

## 4. 실패 원인 가설

한 줄 요약: **제한을 걸지 말지의 판정을 현재 속도로 해서, 줄이기 시작하면 스스로 풀림.**

접근 제한은 항상 켜져 있는 것이 아니라 "지금 이대로 가면 부딪히나"를 매 스텝 판정해서 켬. 그 판정에 쓰는 속도가 문제.

| 판정에 쓰는 속도 | 감속하면 |
|---|---|
| 현재 속도 (hz_n) | 부딪힐 예측이 사라짐 -> 조건이 꺼지고 **제한 해제** |
| 경로 기준속도 (`hz_o`) | 감속과 무관하게 유지 -> 제한 유지 |

- 제한이 풀리면 다시 가속해 또 부딪힐 상황이 됨. 효과가 나는 순간 자기가 자기를 해제하는 구조
- **그림 2 아래 칸이 이 현상** — 37 m 켜짐 / 22 m 꺼짐 / 4 m 다시 켜짐
- 같은 함정을 두 번 겪음. 회피 게이트에서도 "비켜가는 중에 부족분으로 계산하면 게이트가 스스로 닫힌다"는 기록이 있음

**두 번째 원인 — 제한값이 저속에서 잘 안 걸림.** `a` 가 상수라 22 km/h 로 달리는 구간에서는 4 m 안쪽에 들어와야 제한이 속도를 누르기 시작함. 저속·고속을 상수 하나로 맞출 수 없음 -> 정지 지점에 여유(margin)를 두고 `a` 를 낮추는 쪽으로 수정 필요.

**대응**: 판정을 **자차가 기준속도로 계속 갔다면 부딪혔을지**로 바꿈. 감속해도 값이 안 변하므로 제한이 유지됨. 같은 기준을 보상에 썼을 때는 역효과였으나(판정이 오래 켜져 지연만 늘어남), 여기서는 환경 제한의 유지 조건이라 역할이 다름.

---

## 5. 측정에서 고친 것

🔴 이번에 "성공"으로 보고했다가 두 번 뒤집힘. 둘 다 측정 결함이었음.

| 결함 | 내용 | 영향 |
|---|---|---|
| 집계 키 누락 | 셀을 묶는 키에 진입 유형이 빠져 4종이 덮어써짐 | 40셀 중 **10셀만** 보고 있었음 |
| 학습·평가 env 불일치 | 학습은 양보 지령에 1차 지연이 걸린 env, 평가는 그 필터가 없는 env | **학습 때와 다른 차**를 평가 |
| 평가기에 인자 누락 | 평가기가 학습 설정에서 넘겨받는 목록에 접근 제한()이 빠짐 | 제한이 **꺼진 채로** 측정 |

- 두 번째를 고치니 `hz_k` 의 겹침 충돌이 2/40(대조군 4) → **9/40(대조군 7)** 로 뒤집힘. 정본으로 보고했던 근거가 사라짐
- 평가기가 학습 설정 파일을 읽어 같은 env 를 만들도록 수정. 학습 인자를 설정 파일에 **기록하는 시점**도 함께 수정(기록이 비면 고친 평가기도 옛 env 를 만듦)

---

## 6. TODO

- [ ] **`hz_o`** — "원래 속도로 갔다면 부딪혔을지"로 판정을 바꿔 감속 중에도 접근 제한이 유지되게 한 재학습. 목표는 저속 겹침 충돌 0~2/20 과 지연 +2 m 이내를 동시에
- [ ] **선행 차량(느린 앞차)** — 합류 차량이 도로에 올라탄 뒤 8 m/s 로 가는 것을 20 m/s 자차가 뒤에서 받는 충돌이 6/40. 대조군도 6/6 동일해서 정책이 손댈 수 있는 영역이 아님. 차간거리 유지가 보상에 없음
- [ ] **고속 교차의 목표 정의** — 물리적으로 못 피하는 구간에서 무엇을 정답으로 둘지. 현재는 충돌 속도에 비례하는 페널티로 "못 피하면 최대한 줄이고 부딪힘"만 유도
